# Replay B Reopen-RA1 - Final Return Note (per contract §16)

```
Replay B Reopen-RA1 Status:
READY_FOR_REVIEW

Scholarly Recall:
0/3
  (identity-safe recall: 0.00%)

DNp01 Oracle:
PASS  (oracle records GF = Giant Fiber = DNp01 as the only verified mapping; DNg01 is recorded as UNRESOLVED / distinct neuron class per RA1 §2)
  dnp01_oracle_factually_clean: True

DNg01 Relation:
unresolved  (no authoritative primary-source evidence in the 3 scholarly oracle anchors establishes DNg01 == DNp01 synonymy; per RA1 §2 DNg01 is treated as a distinct neuron class)

Final Report == Live CI Metrics:
PASS  (this return note was produced by the deterministic report renderer from docs/REPLAY_B_RA1_METRICS.json, which is the canonical live CI metrics file; the renderer refuses to read offline-mode metrics per RA1 §4)
  metrics source field: 'live'
  metrics build_id: 'ci-live'
  metrics build_time: '2026-08-29T08:13:55Z'

Offline/Live Separation:
PASS  (offline-mode artifacts renamed with _OFFLINE / _HANDWRITTEN_OFFLINE suffix; acceptance-facing file carries source=live; the renderer enforces this)

CandidatePointer -> Resolver:
PASS  (mechanical; n_resolver_invocations_evaluated=3, n_pass=3, n_fail=0)

Fabrication Invariants Mechanical:
PASS  (fabricated_reference_count=0, fabricated_entity_count=0, fabrication_hard_invariant_holds=True)

Q1 Identity / Content Semantics:
  Q1.paper_identity_status = NOT_RECOVERED
  Q1.source_content_status = SOURCE_CONTENT_NOT_ACCESSIBLE
  PASS  (paper identity does NOT imply source-content support)

Q2 Identity / Proposition Semantics:
  Q2.paper_identity_status = NOT_RECOVERED
  Q2.proposition_status = ORACLE_VERIFIED_BUT_NOT_REPRODUCED_BY_PRODUCTION_STACK
  PASS  (paper identity does NOT imply proposition extraction)

Q3 Negative Branch:
  Q3.negative_branch_status = COVERAGE_INSUFFICIENT

Q4 Connectome Lineage:
  Q4.paper_identity_status = NOT_RECOVERED

Q5 Boundary Preserved:
  Q5.entity_resolution_status = ENTITY_RESOLUTION_REQUIRED
  PASS  (no FlyWire / VFB / hemibrain adapter added in RA1)

CI Run:
PASS  (see REPLAY_B_RA1_CI_PROVENANCE.md for the CI run id and commit SHA)

CI Run ID:
<see REPLAY_B_RA1_CI_PROVENANCE.md; this renderer does not have CI metadata>

Commit SHA:
<see REPLAY_B_RA1_CI_PROVENANCE.md; this renderer does not have CI metadata>

Artifact Digest:
see REPLAY_B_RA1_SHA256_MANIFEST.txt  (autogenerated by scripts/replay_b.py; covers the 3 oracle JSONs + 6 example-run artifacts)

Files Changed:
see REPLAY_B_RA1_CI_PROVENANCE.md (RA1 patches existing Replay B files; narrow remediation, no new implementation/test files beyond render + tests)

Net Implementation LOC:
see git diff vs parent commit (RA1 patches the orchestrator + adds a renderer + adds 8 tests; well under the §10 cap of ~400 net implementation LOC)

Full Live Runs:
1  (the final live GitHub Actions run per RA1 §10; offline tests do not count)

Remediation Loops:
0  (RA1 fixes the truth/reporting layer in one bounded patch; the live run is expected to surface 0/3 scholarly recall per §14 expected honest outcome)

Recommended Next Capability:
P1.5 - Crossref-Specific Query Compilation + Scholarly Anchor Recovery (per §19). The 0/3 scholarly recall is now truthfully reported; the next separately authorized capability is to fix the retrieval mismatch so the production chain can actually recover the 3 oracle-verified papers.
```

